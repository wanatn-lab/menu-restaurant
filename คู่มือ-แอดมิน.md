# คู่มือหน้าแอดมิน (แทนที่ Google Sheet)

เว็บนี้มีหน้าแอดมินในตัวแล้วที่ **`/admin`** เช่น `https://jinko-menu.netlify.app/admin`

ใช้แก้ข้อความ รูป และวิดีโอของเว็บได้เลย ไม่ต้องแก้โค้ด ไม่ต้องใช้ Google Sheet อีกต่อไป

---

## วิธีใช้งาน

1. เปิดลิงก์ `/admin` — จะเจอหน้า **เข้าสู่ระบบ** ก่อนเข้าใช้งานเสมอ
   - ชื่อผู้ใช้: `jinko`
   - รหัสผ่าน: `j12345678`
   - ล็อกอินสำเร็จครั้งแรกแล้ว เครื่อง/เบราว์เซอร์เดิมจะจำไว้ให้อัตโนมัติ ครั้งต่อไปเปิด `/admin` จะเข้าหน้าจัดการเนื้อหาได้ทันทีโดยไม่ต้อง login ซ้ำ (จนกว่าจะกด "ออกจากระบบ" หรือเคลียร์ข้อมูลเบราว์เซอร์)
2. แก้ข้อมูลที่ต้องการในแต่ละส่วน:
   - **แบนเนอร์ประกาศด้านบนเว็บ** — ข้อความแถบบนสุดของหน้าแรก/เกี่ยวกับเรา
   - **หน้าแรก (Hero)** — หัวข้อใหญ่ คำโปรย และรูปภาพ
   - **หน้าเกี่ยวกับเรา (Hero)** — หัวข้อใหญ่ พารากราฟ 2 ช่อง และรูปภาพ
   - **เมนูอาหาร** — เพิ่ม/ลบ/แก้/เรียงเมนูได้ทีละจาน ใส่ชื่อ ราคา หมวดหมู่ คำอธิบาย ลิงก์รูป ลิงก์วิดีโอ และติ๊ก "แนะนำ" ได้ — ข้อมูลส่วนนี้จะไปแสดงที่หน้า `/menu-live` ที่ส่งให้ลูกค้าดูได้ทันที
3. กดปุ่ม **"บันทึกการเปลี่ยนแปลง"** ที่แถบล่างสุดของหน้า
4. บันทึกสำเร็จแล้ว รูปหน้าเว็บจริงจะอัปเดตทันที (กด refresh หน้าเว็บเพื่อดูผล)

## รูปภาพและวิดีโอ

- **รูปภาพ**: ใส่เป็นลิงก์ URL เหมือนเดิม (อัปโหลดขึ้น Google Drive/Facebook/IG แล้วคัดลอกลิงก์รูป หรือใช้บริการฝากรูปอื่นก็ได้) วิธีดึงลิงก์รูปจาก Google Drive ดูได้ในไฟล์ `คู่มือ-เมนูไลฟ์.md` (ขั้นตอนที่ 2)
- **วิดีโอ**: ใส่ลิงก์ YouTube (แนะนำอัปโหลดแบบ Unlisted) หรือลิงก์ไฟล์ .mp4 ก็ได้ ปล่อยว่างได้ถ้าเมนูนั้นไม่มีวิดีโอ

## รหัสผ่านแอดมิน

ชื่อผู้ใช้และรหัสผ่านถูกตั้งไว้เป็นตัวแปรลับ (`ADMIN_USERNAME` และ `ADMIN_PASSWORD`) บนระบบ Netlify ของเว็บนี้ ถ้าต้องการเปลี่ยน:

1. เข้า [app.netlify.com](https://app.netlify.com) → เลือกไซต์ **jinko-menu**
2. ไปที่ **Site configuration → Environment variables**
3. แก้ค่าตัวแปร `ADMIN_USERNAME` และ/หรือ `ADMIN_PASSWORD` เป็นค่าใหม่ — มีผลทันทีโดยไม่ต้อง deploy ใหม่
4. ผู้ใช้ที่จำ login เดิมไว้ในเครื่อง (localStorage) จะถูกระบบตรวจพบว่าค่าที่จำไว้ใช้ไม่ได้แล้วในการเปิดครั้งต่อไป และจะให้ login ใหม่ด้วยชื่อผู้ใช้/รหัสผ่านล่าสุดโดยอัตโนมัติ

## ถ้าไม่มีใครกรอกข้อมูลผ่านแอดมินเลย

หน้าเว็บจะแสดงเนื้อหาตัวอย่างเดิม (เหมือนที่เห็นอยู่ตอนนี้) ไปก่อน จนกว่าจะมีการบันทึกผ่าน `/admin` ครั้งแรก

## ข้อควรระวัง

- อย่าส่งลิงก์ `/admin` หรือรหัสผ่านให้คนอื่นที่ไม่ใช่เจ้าของร้าน
- หน้า `/admin` ถูกกันไม่ให้ติด Google Search ไว้แล้ว (robots.txt + noindex)
